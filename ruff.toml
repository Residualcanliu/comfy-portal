# 仓库根目录 ruff 配置（CI 从根目录跑 ruff，需在此提供统一配置）
line-length = 100

[lint.isort]
force-sort-within-sections = true

[lint.per-file-ignores]
# FastAPI 的 Depends() 在默认参数中是标准写法（B008 误报）；
# 产物文件写入是小文件阻塞，M1 阶段可接受（ASYNC230）
"apps/api/app/**/*.py" = ["B008", "ASYNC230"]
