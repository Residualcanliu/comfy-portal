[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "comfyportal-shared"
version = "0.1.0"
description = "ComfyPortal 任务状态机 + SSE 事件 + DTO（Python 侧）"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.5"]

[tool.setuptools]
packages = ["comfyportal_shared"]
