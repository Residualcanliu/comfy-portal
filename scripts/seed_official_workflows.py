"""种子 4 个官方工作流（规格书 §2，admin 种子，新用户一键试跑）。

SD1.5 文生图（GGUF Q8）/ SDXL 文生图（fp16）/ FLUX.1-dev（fp8）/ img2img（SDXL + 4x-UltraSharp）。
工作流存 ComfyUI API 格式 JSON，slots 写入 workflows.slots JSONB。

TODO(M1): 实现。
"""
